<div class="experience-progress" aria-hidden="true">
  <span></span>
</div>

<canvas
  class="meteor-cursor-trail"
  data-meteor-cursor-trail
  aria-hidden="true"
></canvas>

<div class="code-atmosphere" aria-hidden="true">
  <div class="code-atmosphere__pointer"></div>
  <div class="code-aurora code-aurora--cyan"></div>
  <div class="code-aurora code-aurora--aqua"></div>
  <div class="code-atmosphere__grid"></div>
  <div class="shooting-stars">
    <i style="--star-x: 82%; --star-y: 9%; --star-size: 7rem; --star-cycle: 17s; --star-delay: -2s"></i>
    <i style="--star-x: 96%; --star-y: 28%; --star-size: 5.5rem; --star-cycle: 21s; --star-delay: -13s"></i>
    <i style="--star-x: 68%; --star-y: 48%; --star-size: 8rem; --star-cycle: 19s; --star-delay: -8s"></i>
    <i style="--star-x: 91%; --star-y: 67%; --star-size: 6rem; --star-cycle: 23s; --star-delay: -18s"></i>
    <i style="--star-x: 57%; --star-y: 16%; --star-size: 4.5rem; --star-cycle: 25s; --star-delay: -6s"></i>
  </div>
  <div class="code-nodes">
    <i style="--node-x: 8%; --node-y: 24%; --node-delay: 0s"></i>
    <i style="--node-x: 18%; --node-y: 68%; --node-delay: -1.1s"></i>
    <i style="--node-x: 32%; --node-y: 14%; --node-delay: -2.4s"></i>
    <i style="--node-x: 47%; --node-y: 82%; --node-delay: -0.6s"></i>
    <i style="--node-x: 61%; --node-y: 31%; --node-delay: -3.2s"></i>
    <i style="--node-x: 74%; --node-y: 73%; --node-delay: -1.8s"></i>
    <i style="--node-x: 88%; --node-y: 18%; --node-delay: -2.8s"></i>
    <i style="--node-x: 94%; --node-y: 54%; --node-delay: -0.3s"></i>
  </div>
  <span class="code-token code-token--braces">{ }</span>
  <span class="code-token code-token--tag">&lt;/&gt;</span>
  <span class="code-token code-token--binary">01</span>
  <span class="code-token code-token--arrow">=&gt;</span>
  <span class="code-token code-token--prompt">_</span>
  <div class="code-atmosphere__scan"></div>
</div>

<div class="code-console-shell" aria-hidden="true">
  <div class="code-console">
    <div class="code-console__bar">
      <i></i><i></i><i></i>
      <span>campuslands.dev</span>
    </div>
    <code><b>$</b> build future<span class="code-console__cursor">_</span></code>
  </div>
</div>

<div class="experience-rail" aria-hidden="true">
  <code>scroll.run()</code>
  <span><i></i></span>
</div>

<style>
  .meteor-cursor-trail {
    position: fixed;
    inset: 0;
    z-index: 45;
    width: 100vw;
    height: 100vh;
    contain: strict;
    pointer-events: none;
    mix-blend-mode: screen;
    transform: translateZ(0);
  }

  .experience-progress {
    position: fixed;
    top: 0;
    right: 0;
    left: 0;
    z-index: 70;
    height: 3px;
    pointer-events: none;
    background: rgb(255 255 255 / 5%);
  }

  .experience-progress span {
    display: block;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, #00aa80, #2caaff 58%, #57bbff);
    box-shadow: 0 0 16px rgb(44 170 255 / 75%);
    transform: scaleX(var(--scroll-progress, 0));
    transform-origin: left center;
    will-change: transform;
  }

  .code-atmosphere {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .code-atmosphere__pointer {
    position: absolute;
    top: var(--pointer-y, 35%);
    left: var(--pointer-x, 70%);
    width: min(38rem, 55vw);
    aspect-ratio: 1;
    border-radius: 999px;
    opacity: 0.28;
    background: radial-gradient(circle, rgb(44 170 255 / 24%), rgb(0 170 128 / 7%) 42%, transparent 70%);
    transform: translate(-50%, -50%);
    transition: top 900ms cubic-bezier(0.16, 1, 0.3, 1), left 900ms cubic-bezier(0.16, 1, 0.3, 1);
    will-change: top, left;
  }

  .code-aurora {
    position: absolute;
    width: 34rem;
    aspect-ratio: 1;
    border-radius: 45% 55% 62% 38%;
    opacity: 0.14;
    filter: blur(80px);
    animation: aurora-drift 15s ease-in-out infinite alternate;
  }

  .code-aurora--cyan {
    top: -14rem;
    right: -10rem;
    background: #2caaff;
  }

  .code-aurora--aqua {
    bottom: -18rem;
    left: -12rem;
    background: #00aa80;
    animation-delay: -7s;
  }

  .code-atmosphere__grid {
    position: absolute;
    inset: -10%;
    opacity: 0.16;
    background-image:
      linear-gradient(rgb(87 187 255 / 18%) 1px, transparent 1px),
      linear-gradient(90deg, rgb(87 187 255 / 18%) 1px, transparent 1px);
    background-size: 72px 72px;
    mask-image: radial-gradient(circle at 50% 38%, black, transparent 72%);
    transform: perspective(700px) rotateX(64deg) translateY(calc(38% + var(--code-scroll, 0px) * 0.025));
    transform-origin: center bottom;
  }

  .shooting-stars {
    position: absolute;
    inset: 0;
    overflow: hidden;
    opacity: 0.72;
    mix-blend-mode: screen;
  }

  .shooting-stars i {
    position: absolute;
    top: var(--star-y);
    left: var(--star-x);
    width: var(--star-size);
    height: 1px;
    border-radius: 999px;
    opacity: 0;
    background: linear-gradient(
      90deg,
      rgb(255 255 255 / 92%) 0%,
      rgb(87 187 255 / 78%) 12%,
      rgb(44 170 255 / 30%) 48%,
      transparent 100%
    );
    filter: drop-shadow(0 0 4px rgb(87 187 255 / 68%));
    rotate: -34deg;
    transform-origin: left center;
    animation: shooting-star var(--star-cycle) var(--star-delay) linear infinite;
    will-change: translate, opacity, scale;
  }

  .shooting-stars i::before {
    position: absolute;
    top: 50%;
    left: -1px;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    content: "";
    background: #fff;
    box-shadow:
      0 0 5px #fff,
      0 0 11px #57bbff,
      0 0 20px rgb(0 170 128 / 65%);
    translate: 0 -50%;
  }

  .shooting-stars i::after {
    position: absolute;
    top: 50%;
    left: 0;
    width: 1.4rem;
    height: 0.35rem;
    border-radius: 50%;
    content: "";
    background: radial-gradient(ellipse, rgb(255 255 255 / 32%), transparent 72%);
    filter: blur(2px);
    translate: -45% -50%;
  }

  .code-token {
    position: absolute;
    color: #57bbff;
    font-family: Menlo, Monaco, Consolas, monospace;
    font-weight: 700;
    line-height: 1;
    opacity: 0.12;
    text-shadow: 0 0 26px rgb(44 170 255 / 30%);
    will-change: transform;
  }

  .code-token--braces {
    top: 18%;
    left: 4%;
    font-size: clamp(4rem, 9vw, 9rem);
    transform: translate3d(0, calc(var(--code-scroll, 0px) * -0.035), 0) rotate(-7deg);
  }

  .code-token--tag {
    top: 58%;
    right: 3%;
    font-size: clamp(3rem, 7vw, 7rem);
    color: #00aa80;
    transform: translate3d(0, calc(var(--code-scroll, 0px) * -0.055), 0) rotate(6deg);
  }

  .code-token--binary {
    top: 33%;
    right: 12%;
    font-size: clamp(2rem, 4vw, 4rem);
    transform: translate3d(0, calc(var(--code-scroll, 0px) * -0.025), 0);
  }

  .code-token--arrow {
    top: 76%;
    left: 10%;
    font-size: clamp(2.5rem, 5vw, 5rem);
    color: #00aa80;
    transform: translate3d(0, calc(var(--code-scroll, 0px) * -0.045), 0) rotate(-5deg);
  }

  .code-token--prompt {
    top: 44%;
    left: 48%;
    font-size: clamp(3rem, 6vw, 6rem);
    animation: code-cursor 1.4s steps(1, end) infinite;
  }

  .code-atmosphere__scan {
    position: absolute;
    top: -20%;
    right: 0;
    left: 0;
    height: 14rem;
    opacity: 0.13;
    background: linear-gradient(180deg, transparent, rgb(44 170 255 / 20%), transparent);
    transform: translateY(calc(var(--code-scroll, 0px) * 0.18));
  }

  .code-nodes {
    position: absolute;
    inset: 0;
  }

  .code-nodes i {
    position: absolute;
    top: var(--node-y);
    left: var(--node-x);
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: #57bbff;
    box-shadow: 0 0 0 0 rgb(87 187 255 / 28%), 0 0 16px #2caaff;
    animation: node-pulse 3.8s ease-out var(--node-delay) infinite;
  }

  .code-console-shell {
    position: fixed;
    bottom: 1.25rem;
    left: 1.25rem;
    z-index: 4;
    pointer-events: none;
    transition:
      opacity 220ms ease,
      translate 340ms cubic-bezier(0.16, 1, 0.3, 1),
      visibility 0s linear;
  }

  .code-console-shell[data-footer-hidden="true"] {
    visibility: hidden;
    opacity: 0;
    translate: 0 calc(100% + 2rem);
    transition:
      opacity 180ms ease,
      translate 300ms cubic-bezier(0.16, 1, 0.3, 1),
      visibility 0s linear 300ms;
  }

  .code-console {
    position: relative;
    width: 15.5rem;
    overflow: hidden;
    border: 1px solid rgb(87 187 255 / 24%);
    border-radius: 0.8rem;
    color: rgb(255 255 255 / 78%);
    background: linear-gradient(135deg, rgb(7 16 43 / 78%), rgb(0 0 81 / 62%));
    box-shadow: 0 1rem 3rem rgb(0 0 31 / 28%), inset 0 1px rgb(255 255 255 / 7%);
    opacity: 0;
    transform: translateY(1rem);
    backdrop-filter: blur(14px);
    animation: console-arrive 700ms 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .code-console__bar {
    display: flex;
    align-items: center;
    gap: 0.32rem;
    padding: 0.42rem 0.65rem;
    border-bottom: 1px solid rgb(255 255 255 / 7%);
    background: rgb(255 255 255 / 3%);
  }

  .code-console__bar i {
    width: 0.38rem;
    height: 0.38rem;
    border-radius: 50%;
    background: #57bbff;
  }

  .code-console__bar i:nth-child(2) {
    background: #2caaff;
  }

  .code-console__bar i:nth-child(3) {
    background: #00aa80;
  }

  .code-console__bar span {
    margin-left: 0.25rem;
    font: 500 0.58rem/1 Menlo, Monaco, Consolas, monospace;
    letter-spacing: 0.08em;
    opacity: 0.58;
  }

  .code-console code {
    display: block;
    padding: 0.62rem 0.72rem 0.7rem;
    font: 600 0.7rem/1 Menlo, Monaco, Consolas, monospace;
  }

  .code-console code b {
    color: #00aa80;
  }

  .code-console__cursor {
    color: #57bbff;
    animation: code-cursor 1s steps(1, end) infinite;
  }

  .experience-rail {
    position: fixed;
    top: 50%;
    right: 1.1rem;
    z-index: 4;
    display: flex;
    align-items: center;
    gap: 0.65rem;
    pointer-events: none;
    transform: translateY(-50%);
  }

  .experience-rail code {
    color: rgb(87 187 255 / 48%);
    font: 600 0.57rem/1 Menlo, Monaco, Consolas, monospace;
    letter-spacing: 0.04em;
    writing-mode: vertical-rl;
  }

  .experience-rail > span {
    position: relative;
    width: 2px;
    height: 8rem;
    overflow: hidden;
    border-radius: 999px;
    background: rgb(255 255 255 / 10%);
  }

  .experience-rail > span i {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: linear-gradient(#2caaff, #00aa80);
    box-shadow: 0 0 12px #2caaff;
    transform: scaleY(var(--scroll-progress, 0));
    transform-origin: top center;
  }

  @keyframes aurora-drift {
    to {
      translate: -5rem 4rem;
      rotate: 14deg;
      scale: 1.12;
    }
  }

  @keyframes node-pulse {
    0%, 100% {
      opacity: 0.25;
      box-shadow: 0 0 0 0 rgb(87 187 255 / 22%), 0 0 10px #2caaff;
    }
    45% {
      opacity: 0.9;
      box-shadow: 0 0 0 9px rgb(87 187 255 / 0%), 0 0 22px #2caaff;
    }
  }

  @keyframes shooting-star {
    0%, 3% {
      opacity: 0;
      translate: 0 0;
      scale: 0.25 1;
    }
    3.6% {
      opacity: 0.82;
    }
    7.5% {
      opacity: 0;
      translate: -36vw 28vh;
      scale: 1 1;
    }
    8%, 100% {
      opacity: 0;
      translate: -36vw 28vh;
      scale: 0.25 1;
    }
  }

  @keyframes console-arrive {
    to {
      opacity: 0.82;
      transform: translateY(0);
    }
  }

  @keyframes code-cursor {
    50% {
      opacity: 0.02;
    }
  }

  @media (max-width: 767px) {
    .meteor-cursor-trail {
      display: none;
    }

    .code-token {
      opacity: 0.075;
    }

    .code-token--binary,
    .code-token--prompt {
      display: none;
    }

    .code-atmosphere__grid {
      background-size: 54px 54px;
    }

    .code-console-shell,
    .experience-rail,
    .code-atmosphere__pointer,
    .code-nodes i:nth-child(even) {
      display: none;
    }

    .shooting-stars {
      opacity: 0.52;
    }

    .shooting-stars i {
      width: min(var(--star-size), 5rem);
    }

    .shooting-stars i:nth-child(n + 4) {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .meteor-cursor-trail {
      display: none;
    }

    .code-token,
    .code-atmosphere__grid,
    .code-atmosphere__scan,
    .code-aurora,
    .code-nodes i,
    .code-console,
    .code-console__cursor,
    .shooting-stars i {
      animation: none;
      transform: none;
    }

    .shooting-stars {
      display: none;
    }

    .code-console {
      opacity: 0.82;
    }
  }
</style>
